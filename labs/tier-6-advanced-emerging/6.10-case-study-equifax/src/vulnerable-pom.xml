<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.equifax</groupId>
    <artifactId>dispute-portal</artifactId>
    <version>4.1.0</version>
    <packaging>war</packaging>

    <name>Equifax Consumer Dispute Portal</name>
    <description>
        Reconstructed dependency configuration of the Equifax consumer dispute
        portal that was breached via CVE-2017-5638. The application used Apache
        Struts 2.3.31, which was vulnerable to RCE via malicious Content-Type
        headers. Apache released Struts 2.3.32 (patched) on March 7, 2017.
        Equifax did not apply the patch. Attackers exploited it starting May 13, 2017.

        FOR EDUCATIONAL PURPOSES -- WeakLink Labs training material
    </description>

    <properties>
        <java.version>1.8</java.version>
        <!--
            VULNERABLE VERSION: Struts 2.3.31
            PATCHED VERSION: Struts 2.3.32 (released March 7, 2017)

            CVE-2017-5638: Remote Code Execution via malicious Content-Type header.
            The Jakarta Multipart parser in Apache Struts 2.3.5 through 2.3.31
            evaluates OGNL expressions from the Content-Type header when an
            exception is thrown during file upload parsing.

            Attack payload example (in Content-Type header):
            Content-Type: %{(#_='multipart/form-data').(#dm=@ognl.OgnlContext@DEFAULT_MEMBER_ACCESS)...}

            This gives the attacker full RCE on the web server.
        -->
        <struts.version>2.3.31</struts.version>
    </properties>

    <dependencies>
        <!-- Apache Struts 2 Core -- VULNERABLE to CVE-2017-5638 -->
        <dependency>
            <groupId>org.apache.struts</groupId>
            <artifactId>struts2-core</artifactId>
            <version>${struts.version}</version>
        </dependency>

        <!-- Struts 2 Convention Plugin -->
        <dependency>
            <groupId>org.apache.struts</groupId>
            <artifactId>struts2-convention-plugin</artifactId>
            <version>${struts.version}</version>
        </dependency>

        <!-- Struts 2 Spring Plugin -->
        <dependency>
            <groupId>org.apache.struts</groupId>
            <artifactId>struts2-spring-plugin</artifactId>
            <version>${struts.version}</version>
        </dependency>

        <!-- Spring Framework -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>4.3.7.RELEASE</version>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-dbcp2</artifactId>
            <version>2.1.1</version>
        </dependency>

        <!-- Servlet API -->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>3.1.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>2.8.1</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.1.0</version>
            </plugin>
        </plugins>
    </build>
</project>
