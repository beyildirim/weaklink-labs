// Postinstall — this script is referenced in the tarball's package.json
// but the registry metadata shows a different (harmless) scripts section
console.log('[crafted-widget] Setting up widget configuration...');
