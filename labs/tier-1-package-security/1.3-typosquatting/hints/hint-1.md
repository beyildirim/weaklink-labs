Look closely at the package name in `install_deps.sh`. Compare it character by character
with the real package name. The letters 'u' and 'e' might not be in the right order...

Try: `cat /app/scripts/install_deps.sh`
