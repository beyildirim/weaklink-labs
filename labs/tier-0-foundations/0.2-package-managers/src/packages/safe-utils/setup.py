from setuptools import setup

setup(
    name="safe-utils",
    version="1.0.0",
    description="A safe utility library for string operations",
    py_modules=["safe_utils"],
    python_requires=">=3.8",
)
