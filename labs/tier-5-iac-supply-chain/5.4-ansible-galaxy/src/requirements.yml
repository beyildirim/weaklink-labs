---
# Ansible Galaxy requirements
# Install with: ansible-galaxy install -r requirements.yml
roles:
  - name: ntp_config
    src: community.ntp_config
    # No version pin -- pulls latest from Galaxy
