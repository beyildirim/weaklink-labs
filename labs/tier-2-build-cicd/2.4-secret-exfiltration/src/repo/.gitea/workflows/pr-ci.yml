name: PR Validation (No Secrets)

on:
  pull_request:
    branches: [main]

# Zero secrets available -- safe to run attacker-controlled code

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: python test_app.py
