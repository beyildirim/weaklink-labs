# Order Processor Dependencies
#
# VULNERABILITY: This requirements.txt is used by the SAM build step.
# If the build environment resolves packages from public PyPI before
# checking the private registry, an attacker can publish a higher-version
# package on public PyPI with the same name as the internal package.
#
# The internal package "acme-order-utils" exists on the private registry
# at version 1.2.3. An attacker publishes "acme-order-utils" version 99.0.0
# on public PyPI. The build step installs 99.0.0 (malicious) instead of
# 1.2.3 (legitimate).

boto3>=1.28.0
botocore>=1.31.0
acme-order-utils>=1.0.0
acme-notification-client>=2.0.0
requests>=2.31.0
